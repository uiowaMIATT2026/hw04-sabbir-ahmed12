# Gaussian Low-Pass Filter
- For this part of the homework, MR head example image from Slicer was used with different values of `sigma` to observe its effect.
- The edges and details within the image was somewhat preserved and visible until `sigma=1.8`. At `sigma=2`, the edges became blurry.
- Beyond `sigma=2`, the image becomes blurry and no detail information was observed.